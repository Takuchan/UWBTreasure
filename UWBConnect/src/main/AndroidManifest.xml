<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".UWBConnectActivity"
            android:exported="false"
            android:label="@string/title_activity_uwbconnect"
            android:theme="@style/Theme.UWBviaSerial">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
        </activity>
    </application>

</manifest>